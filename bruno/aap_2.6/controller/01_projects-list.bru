meta {
  name: Projects list
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/controller/v2/projects/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  SCM projects that synchronize playbooks to the controller.
}
