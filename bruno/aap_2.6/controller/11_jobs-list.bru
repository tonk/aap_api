meta {
  name: Jobs list
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/api/controller/v2/jobs/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Unified job records (playbook runs). Use `?status=running` or `?job_template=<id>`.
}
