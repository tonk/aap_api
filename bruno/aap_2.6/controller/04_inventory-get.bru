meta {
  name: Inventory get by ID
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/controller/v2/inventories/{{inventoryId}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Inventory detail. `related.hosts` and `related.groups` for contents.
}
