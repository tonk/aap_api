meta {
  name: Credential get by ID
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/api/controller/v2/credentials/{{credentialId}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Credential metadata. Input fields are write-only on create/update.
}
