meta {
  name: Current user (EDA)
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/api/eda/v1/users/me/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  EDA `users/me` profile for the bearer identity (may mirror gateway user).
}
