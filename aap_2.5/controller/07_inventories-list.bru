meta {
  name: Inventories list
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/controller/v2/inventories/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Inventories (static, constructed, etc.). Use `?organization=<id>` to narrow.
}
