meta {
  name: Organization get by ID (controller)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/controller/v2/organizations/{{organizationId}}/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Single organization detail including related links.
}
