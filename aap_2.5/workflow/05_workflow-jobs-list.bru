meta {
  name: Workflow jobs list
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/controller/v2/workflow_jobs/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Running or completed workflow jobs. Useful to find the approval id tied to
  a workflow execution (`summary_fields` / `workflow_job_template`).
}
