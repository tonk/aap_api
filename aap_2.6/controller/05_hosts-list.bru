meta {
  name: Hosts list
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/controller/v2/hosts/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Lists hosts across inventories. Filter with `?inventory=<id>`.
}
