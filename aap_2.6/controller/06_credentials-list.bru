meta {
  name: Credentials list
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/controller/v2/credentials/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Credential objects (secrets are not returned in clear text). Filter by `credential_type` or organization.
}
